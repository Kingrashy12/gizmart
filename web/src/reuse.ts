{
  /* {!form.allProducts && (
        <select onChange={(e) => handleCollectionChange(e.target.value)}>
          <option value="">Select a collection</option>
          {Object.keys(productsCollection).map((collection) => (
            <option key={collection} value={collection}>
              {productsCollection[collection].collection}
            </option>
          ))}
        </select>
      )} */
}
